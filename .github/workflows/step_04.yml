name: jPipe Runner Workflow -- Step 03

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'step_04/**'

permissions:
  contents: write
  pull-requests: write

jobs:
  jpipe-runner:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Run jPipe Runner
        uses: jpipe-mcscert/jpipe-runner@feat/v3.1.0
        with:
          version: feat/v3.1.0
          embed_image: "true"
          image_branch: "diagram-images"
          jd_file: step_04/deployable.json
          library: step_04/steps/deployable.py
          github-token: ${{ secrets.GITHUB_TOKEN }}